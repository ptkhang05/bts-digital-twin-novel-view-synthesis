# Báo cáo khôi phục workspace ngày 2026-07-20

## Phạm vi sự cố

Workspace `D:\BTS Digital Twin (Novel View Synthesis)` bị mất khỏi ổ D. Repo
được khôi phục từ GitHub public; raw input được tải lại từ Google Drive và xác
minh bằng manifest đã commit. Báo cáo này phân biệt rõ file gốc, dữ liệu tái sinh
và thông tin chỉ còn dưới dạng provenance.

## Đã khôi phục và xác minh

- Git repo đầy đủ tại commit `11f591e5c4c2a8da8f28e401e96c09ff3f3faf5a`,
  gồm history, remote branches, code, test, CI, docs, manifest và ledger.
- `VAI_NVS_DATA_ROUND2`: tải từ Drive ID
  `1jQ-SYjLJ42UGY2O574j437NvUxFSEF4l`, ingest 1.708 file/1.633.396.970 byte.
  Audit xác nhận 7 scene, 1.653 train, 386 target và manifest SHA-256
  `4839983968385ec56a418909bd70c77a310233b328762db1a2f4bde1c7bcadb8`.
- Môi trường CPU Python 3.13 trong `.bootstrap-venv`; dependency lấy từ các
  constraint đã commit.
- `processed/full`, `processed/holdout` và `processed/holdout-ground-truth`
  được tái sinh bằng hardlink. Holdout có đúng 164 ảnh.
- Cấu trúc local `outputs/candidate`, `outputs/best`, `outputs/.staging` được
  tạo lại.
- Quality gates sau khôi phục: `ruff` pass, 192 test pass/2 skip, wheel và
  sdist build thành công, `pip check` không có dependency hỏng.
- Ledger vẫn giữ record chính thức `vai-nvs-round2-001`: score BTC 70,08,
  ZIP SHA-256 `6f7aa6c9dc9eccf2a9ba62f85f68059d230b307193147307ea081a39fefc3a94`,
  kích thước 173.357.943 byte và toàn bộ metric BTC/holdout 10k.

## Không thể khôi phục byte-for-byte từ nguồn hiện có

- `submission.zip` chính thức và thư mục `outputs/best`: Git cố ý ignore các
  artifact này; Recycle Bin ổ D không chứa chúng. ZIP chỉ có thể lấy lại từ cổng
  BTC nếu cổng cho tải bản đã nộp, từ một backup khác, hoặc tái train/render.
- Checkpoint, render và log GPU: không được push lên Git theo artifact policy và
  VM cũ không còn được xem là nguồn khả dụng.
- Hai Markdown yêu cầu BTC gốc. Manifest còn lưu đúng tên, kích thước và hash,
  nhưng blob gốc chưa tìm thấy trong Git history/attachments:
  - `Yêu cầu BTC BTS 1.md`: 8.989 byte,
    SHA-256 `2b957720c461e20663d45c0586e96ea4b60f887296cbed11d3e9987b62213369`.
  - `Yêu cầu BTC BTS 2.md`: 3.252 byte,
    SHA-256 `a51954e1738259c6d89810d79dc80398c7919731471ae318181448fea1cdc0e3`.
  Nội dung vận hành đã được bảo toàn dưới dạng dẫn xuất tại
  `docs/current-btc-spec.md`; không được coi bản dẫn xuất là hai file gốc.

## Kết quả thí nghiệm chỉ còn provenance

Các metric dưới đây được khôi phục từ transcript người dùng. JSON/log/checkpoint
gốc đã mất, nên chúng không có hash artifact và không được dùng để promote một
candidate:

| Thử nghiệm | Scene/mốc | Delta proxy (điểm scene) | Kết luận |
| --- | --- | ---: | --- |
| antialiased | toàn holdout 10k | -0,530957 | classic thắng |
| antialiased | bonsai 30k | -0,069357 | classic thắng |
| explicit cull alpha 0,005 | chair 30k | -0,525974 | đây là default của `splatfacto-big`, nên là repeat nhiễu chứ không phải A/B |
| SSIM lambda 0,3 | chair 10k | -0,213055 | loại |
| SSIM lambda 0,1 | chair 10k | -0,712666 | loại |
| densify grad 0,0004 | chair 10k | +0,496505 | thắng tạm ở 10k |
| densify grad 0,0004 | HCM0644 10k | +0,310111 | thắng tạm ở 10k |
| densify grad 0,0004 | chair 30k | -0,010059 | không vượt baseline 30k |
| stop split 25k | chair 30k | -0,442699 | loại |
| SH degree 4 | chair 30k | +0,116030 | yếu/chưa kết luận |
| SH degree 4 | HCM0644 10k | +0,274341 | thắng tạm ở 10k |
| SH degree 4 | HCM0644 30k | -5,281461 | không an toàn; loại |

## Baseline hiện hành

Giữ nguyên `quality` = Nerfstudio 1.1.5 `splatfacto-big`, classic, 30.000
iteration, seed 42, pose normalization off, distortion `auto`, JPEG quality 95.
Preset `splatfacto-big` override model base defaults thành cull alpha `0.005` và
densify grad `0.0005`; scheduler means kết thúc ở 30.000 step. Không đề xuất
40k nếu chưa thiết kế và xác minh lại toàn bộ scheduler.

Trước lần GPU tiếp theo cần lưu config hiệu lực và metric JSON vào provenance
đã commit ngay sau mỗi thí nghiệm. Screening 10k chỉ được dùng để loại nhanh;
mọi candidate phải thắng ở 30k trên ít nhất hai scene và vượt rõ nhiễu repeat.
